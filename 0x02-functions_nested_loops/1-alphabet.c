#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - prints the alphabet in lowercase, followed by a new line.
 */

int main(void)
{
	int i;

	for (i = 97; i <= 122; i++)

	putchar(i);

	putchar('\n');

	return (0);

}
